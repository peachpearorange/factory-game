[package]
name = "factory_game"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.19.1"
avian3d = "0.7.0"
fidget = { version = "0.5.0", default-features = false, features = ["mesh"] }
nalgebra = "0.35.0"
bevy_hanabi = { version = "0.19.0", default-features = false, features = ["3d"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
